import React from 'react';

const EmptyState = () => (
  <div className="h-full w-full flex flex-col justify-center items-center text-gray-400 font-bold p-6">
    <div>Playlist is empty</div>
    <div>Add some songs from results</div>
  </div>
);

export default EmptyState;
